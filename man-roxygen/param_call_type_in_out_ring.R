#' @param call_type_in_out_ring \code{"all"} includes calls of type OUTGOING, INCOMING, RINGING.
#'   Can also be \code{"OUTGOING"}, \code{"INCOMING"} or \code{"RINGING"}.
#'   Multiple types are accepted as input. In this case, the return value is the composite result for these types.
