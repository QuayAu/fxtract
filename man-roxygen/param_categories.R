#' @param categories The app categories which should be considered.
#' Must be either "all" or a vector of strings specifying categories.
#' If categories = "all", every value of the variable \code{apps.appStoreCategory} which occurs in the dataset will be chosen.
#' Multiple categories are accepted as input. In this case, the return value is the composite result for these categories.
