#' @param apps The app package names which should be considered.
#' Must be either "all" or a vector of strings specifying app package names.
#' If apps = "all", every value of the variable \code{apps.packageName} which occurs in the dataset will be chosen.
#' Multiple package names are accepted as input. In this case, the return value is the composite result for these apps.
