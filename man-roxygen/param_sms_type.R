#' @param sms_type \code{"all"} includes all SMS (OUTBOX, SENT, and INBOX).
#'   Can also be \code{"OUTBOX"}, \code{"SENT"}, or \code{"INBOX"}.
#'   Multiple types are accepted as input. In this case, the return value is the composite result for these types.
