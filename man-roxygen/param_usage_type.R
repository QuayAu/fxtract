#' @param usage_type \code{"all"} includes all usage types (USING, CHECKING and NOTIFICATION).
#'   Can also be \code{"USING"}, \code{"CHECKING"} or \code{"NOTIFICATION"}.
#'   Multiple types are accepted as input. In this case, the return value is the composite result for these types.
