#' @param call_type_in_and_out \code{"all"} includes all actual calls (OUTGOING and INCOMING).
#'   Can also be \code{"OUTGOING"} or \code{"INCOMING"}.
#'   Multiple types are accepted as input. In this case, the return value is the composite result for these types.
