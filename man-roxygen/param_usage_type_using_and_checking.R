#' @param usage_type_using_and_checking \code{"all"} includes all usage types (USING and CHECKING).
#'   Can also be \code{"USING"} or \code{"CHECKING"}.
#'   Multiple types are accepted as input. In this case, the return value is the composite result for these types.
