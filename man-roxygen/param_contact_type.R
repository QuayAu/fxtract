#' @param contact_type \code{"all"} includes both types of contacts (calls and SMS).
#'   Can also be \code{"PHONE"} or \code{"SMS"}.
#'   Multiple types are accepted as input. In this case, the return value is the composite result for these types.
